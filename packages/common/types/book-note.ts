// Free-form note on a whole book, written from the web UI (not synced from KoReader)
export type BookNote = {
  id: number;
  book_md5: string;
  text: string;
  created_at: string;
  updated_at: string;
};
